server {
    listen 80;
    server_name {{ jenkins_domain }};

    location / {
        return 200 'Jenkins temporary HTTP config for certificate validation.';
        add_header Content-Type text/plain;
    }
}