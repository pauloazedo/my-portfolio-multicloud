# ansible/roles/prod_site/tasks/main.yml
# PROD Site Blue/Green Deployment Logic with Fallback

#####################################################################
# Step 1: Ensure Docker is installed on the PROD host
#####################################################################
- name: Ensure Docker is installed
  ansible.builtin.include_role:
    name: docker
  tags: always

#####################################################################
# Step 2: Authenticate with OCIR (Oracle Container Registry)
#####################################################################
- name: Check if already logged into OCIR
  ansible.builtin.command: docker info
  register: docker_info_check
  failed_when: false
  changed_when: false

- name: Login to OCIR (if not already logged in)
  ansible.builtin.command:
    cmd: >
      docker login iad.ocir.io
      -u {{ ocir_user }}
      -p {{ ocir_token }}
  when: docker_info_check.stdout is not search('iad.ocir.io')
  no_log: true
  changed_when: false

#####################################################################
# Step 3: Discover the latest prod-site image tag from OCIR
#####################################################################
- name: Get latest prod-site image tag from OCIR
  ansible.builtin.shell: |
    set -o pipefail
    curl -s -H "Authorization: Bearer {{ ocir_token }}" \
      https://iad.ocir.io/v2/{{ ocir_namespace }}/prod-site/tags/list |
    jq -r '.tags | sort | reverse | .[0]'
  register: latest_tag_result
  failed_when: latest_tag_result.rc != 0 or latest_tag_result.stdout == ""
  changed_when: false

- name: Set latest prod-site tag as fact and define blue/green image references
  ansible.builtin.set_fact:
    latest_prod_site_tag: "{{ latest_tag_result.stdout }}"
    prod_site_blue_image: "iad.ocir.io/{{ ocir_namespace }}/prod-site:{{ latest_tag_result.stdout }}"
    prod_site_green_image: "iad.ocir.io/{{ ocir_namespace }}/prod-site:{{ latest_tag_result.stdout }}"

- name: Show image tag being deployed
  ansible.builtin.debug:
    msg: "Deploying prod-site version {{ latest_prod_site_tag }} to blue/green"

#####################################################################
# Step 4: Check image availability
#####################################################################
- name: Check availability of versioned prod-site images
  ansible.builtin.include_tasks: check_images.yml
  tags: fallback

#####################################################################
# Step 5: Fallback logic (build and push if image not found)
#####################################################################
- name: Build and push fallback image
  ansible.builtin.include_tasks: run-fallback.yml
  when:
    - force_fallback | default(false)
    - not blue_image_available and not green_image_available
  tags:
    - fallback
    - force-fallback

#####################################################################
# Step 6: Run containers
#####################################################################
- name: Deploy to blue environment
  ansible.builtin.include_tasks: run-blue.yml
  when: prod_site_target in ["both", "blue"]

- name: Deploy to green environment
  ansible.builtin.include_tasks: run-green.yml
  when: prod_site_target in ["both", "green"]

#####################################################################
# Step 7: Cleanup fallback containers
#####################################################################
- name: Cleanup fallback resources for blue
  ansible.builtin.include_tasks: cleanup-blue.yml
  when:
    - prod_site_target in ["both", "blue"]
    - not blue_image_available and not green_image_available
  tags: fallback

- name: Cleanup fallback resources for green
  ansible.builtin.include_tasks: cleanup-green.yml
  when:
    - prod_site_target in ["both", "green"]
    - not blue_image_available and not green_image_available
  tags: fallback

#####################################################################
# Step 8: Docker system cleanup
#####################################################################
- name: Final Docker system cleanup to reclaim disk space
  ansible.builtin.import_tasks: docker-prune.yml
  tags: [prod_site, cleanup]
